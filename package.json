{
  "name": "grunt-beanstalk-api",
  "version": "1.0.0",
  "description": "Simple beanstalk api interface",
  "main": "tasks/index.js",
  "directories": {
    "test": "test"
  },
  "devDependencies": {
    "babel": "^5.0.10",
    "chai": "^2.2.0",
    "grunt": "^0.4.5",
    "jshint": "^2.6.3",
    "mocha": "^2.2.4"
  },
  "scripts": {
    "lint": "jshint src/*/*.js",
    "es6": "babel src --out-dir .",
    "es6-watch": "babel src --out-dir . --watch",
    "unit": "npm run es6",
    "test": "npm run lint && npm run unit"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "beanstalk-api": "git://github.com/WeareJH/beanstalk-api",
    "object-assign": "^2.0.0"
  }
}
